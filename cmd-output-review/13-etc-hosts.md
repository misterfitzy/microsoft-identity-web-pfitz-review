# /etc/hosts File

```bash
runner@github-runner:~$ ls -la /etc/hosts
-rw-r--r-- 1 root root 326 Nov 23 22:15 /etc/hosts
```

## Summary
Standard hosts file with basic localhost entries for IPv4 and IPv6:
- `127.0.0.1` → localhost
- `::1` → ip6-localhost, ip6-loopback
- IPv6 special addresses for local network and multicast
