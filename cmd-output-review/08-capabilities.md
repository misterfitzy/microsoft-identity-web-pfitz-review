# Capabilities Check

```bash
runner@github-runner:~$ capsh --print | grep Current
Current: =
Current IAB:
```

## Summary
The current process has no special capabilities set. Running with standard user permissions (no elevated capabilities like CAP_NET_ADMIN, CAP_SYS_ADMIN, etc.).
