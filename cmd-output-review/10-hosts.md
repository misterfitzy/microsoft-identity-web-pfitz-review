# /etc/hosts File

## Command
```bash
ls -la /etc/hosts
```

## Output
```
-rw-r--r-- 1 root root 326 Nov 23 22:15 /etc/hosts
```

## Description
The /etc/hosts file exists with standard permissions. It contains localhost mappings and IPv6 localhost definitions.
